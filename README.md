# TCP-Port-Scanner
A lightweight TCP port scanner built with Python using only socket and time modules. Supports custom IP, hostname input, and port range selection with full exception handling and input validation. No third-party libraries required.
